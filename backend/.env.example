# Variables de entorno requeridas para levantar FastAPI
MONGODB_URI="mongodb+srv://<user>:<password>@cluster.mongodb.net/cleanpool?retryWrites=true&w=majority"
SECRET_KEY="tu_secreto_super_seguro_de_32_caracteres_min_para_jwt"
API_KEY="clave_secreta_para_esp8266" # Requerido: Clave que usará el ESP8266 en header X-API-KEY

# Opcional — MongoDB dedicado a logs estructurados (si se omite, no se persiste en BD)
# MONGODB_LOGS_URI="mongodb+srv://<user>:<password>@cluster.mongodb.net/?retryWrites=true&w=majority"

