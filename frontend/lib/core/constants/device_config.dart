/// Identificadores del ESP8266 y topic MQTT (cleanpool/{slug}/temperatura).
class DeviceConfig {
  DeviceConfig._();

  static const String deviceId = 'cleanpool-001';
  static const String mqttTopicSlug = 'piscina-1';
}
